import 'dart:async';

class BreezServer {
  Future<String> registerDevice(String notificationToken, String nodeId) async {
    throw UnimplementedError();
  }

  Future<String> uploadLogo(List<int> imageBytes) async {
    throw UnimplementedError();
  }

  Future<void> checkVersion() async {
    throw UnimplementedError();
  }
}
